# requirements.txt
# Core LLM / vision generation
openai==2.43.0

# Multimodal retrieval (ColPali)
colpali-engine==0.3.7
transformers==4.47.0

# torch is intentionally NOT pinned to a CUDA build here.
# This installs CPU-only torch so the rest of the stack works immediately.
# GPU (RTX 5070, Blackwell sm_120) requires a separate cu128 install — see DEPLOY notes.
torch==2.7.0

# Vector store
qdrant-client==1.12.1

# Text chunking only (lightweight — no agent/orchestration dependencies)
llama-index-core==0.12.10

# API server
fastapi==0.115.6
uvicorn[standard]==0.32.1

# PDF rendering to images + text extraction
pypdfium2==4.30.0
pillow==11.0.0

# Config + validation
python-dotenv==1.0.1
pydantic==2.10.4
pydantic-settings==2.7.0

# Testing
pytest==8.3.4
httpx==0.28.1
